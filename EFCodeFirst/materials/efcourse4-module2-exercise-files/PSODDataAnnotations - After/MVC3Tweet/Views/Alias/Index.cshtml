@model IEnumerable<DomainClasses.Alias>

@{
    ViewBag.Title = "Aliases";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Name
        </th>
        <th>
            UserName
        </th>
        <th>
            Email
        </th>
        <th>
            Bio
        </th>
        <th>
            CreateDate
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreateDate,"Date")
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AuthorKey }) |
            @Html.ActionLink("Tweets", "..//Tweet//Index", new {authorId=item.AuthorKey }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.AuthorKey })
        </td>
    </tr>
}

</table>
